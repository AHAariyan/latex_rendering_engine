[package]
name = "mathffi"
description = "C ABI for mathcore: the surface every platform binding (Kotlin, Swift, Dart, JS) is built on"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[lib]
name = "mathcore_ffi"
crate-type = ["cdylib", "staticlib", "rlib"]

[features]
# Passes through to mathcore: full shaping for Arabic and the Indic scripts.
complex-text = ["mathcore/complex-text"]
default = ["bundled-font"]
# Embed Latin Modern Math so callers need no font file.
bundled-font = []

[dependencies]
mathcore = { path = "../mathcore" }
ttf-parser = { workspace = true }
